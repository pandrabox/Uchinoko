# プライバシーポリシー

*[English](PRIVACY.en.md)*

最終更新: 2026-07-31

Uchinoko for Palworld(以下「本ツール」)は、VRChatアバターをPalworld用のプレイヤーモデルへ変換するためのWindowsデスクトップアプリです。本ツールは**ローカル完結**で動作し、変換対象のアバターの3Dモデルデータ(メッシュ・テクスチャ等)そのものを外部へ送信することはありません。

本ツールが外部と通信するのは、次の2つの場合だけです。

## 1. 問い合わせ機能(ユーザーが明示的に送信操作をした場合のみ)

アプリ内の「問合せ」ボタンを押すと、送信内容を確認・編集できる画面が表示されます。ここで送信ボタンを押した場合にのみ、次の情報が `https://report.osakishokai.com` へ送信されます。**送信は常にこの明示的な操作が必要であり、自動送信は一切行われません。**

送信される項目:

- ツールのバージョン
- 入手元(BOOTH / itch.io / GitHub / 開発版のいずれか)
- 送信時のローカル日時
- OSの種別(レジストリから取得した製品名・ビルド番号。例:「Windows 11」「build 26200.xxxx」)
- UI表示言語・OSのロケール設定
- 選択中アバターの**ファイル名のみ**(フォルダの絶対パスは含みません)
- 検出したPalworldのバージョンとの互換性判定結果
- 検出した他のpak MODの一覧
- その回のツール操作の実行ログ全文

これらはすべて `app\DiveToPalworld.cs` の `BuildDiagnosticsText()` が組み立てます。アバターのメッシュ・テクスチャ等のモデルデータそのものは含まれません。

### 自動的な個人情報マスキング

送信直前に、次のマスキング処理(`SanitizeForClipboard()`)が自動的に適用されます。

- 既知のユーザーフォルダ(`%LOCALAPPDATA%` 等)のパスを伏字化
- Steamの64bit ID(`7656119` で始まる17桁の数字)を伏字化
- Windowsのユーザーアカウント名・PC名を伏字化
- 上記に当てはまらない任意ドライブの絶対パス・ネットワークパスも、長さと拡張子だけを残した形(例: `<path len=42 ext=.prefab>`)へ変換し、実際のパス文字列は残しません

### 任意項目

- **画像の添付**: 問い合わせ用のWebフォームからは、任意で画像を1枚添付できます。添付画像はGPS等のExif位置情報を保存前に自動的に除去したうえで保存します。
- **通知メールの登録**: 送信後に表示される専用ページで、返信通知を受け取るためのメールアドレスを任意で登録できます。登録した場合のみ、返信が届いた際に通知メールを送信します(第三者サービス Resend を利用します)。登録しなくても、専用ページのURLを控えておけば、そこで返信状況を確認できます。

## 2. 更新確認(自動・無音)

起動のたびに、更新の有無を確認するための静的ファイル `https://dl.osakishokai.com/versions.json` を自動的に取得します。これは非同期・低優先度の通信で、オフラインや失敗時は無音のまま処理を継続します。このリクエスト自体にはユーザーやアバターを特定する情報は含まれません(通常のWebアクセスと同様、IPアドレスやUser-Agentがサーバー側のアクセスログに残る可能性はあります)。

## 3. 保存先・保存期間

問い合わせ機能で送信されたデータは、Cloudflare Workers上で稼働する開発者運用のサービス(Cloudflare D1データベース + R2オブジェクトストレージ)に保存されます。

各問い合わせには8文字のランダムなIDが割り当てられ、そのIDを含むURL(`https://report.osakishokai.com/r/<ID>`)を知っている人であれば誰でもそのスレッドを閲覧・返信できます(パスワード等の追加認証はありません)。このURLは他人と共有しないようご注意ください。

**保存期間について、現在の実装には自動的な削除・保持期限の仕組みはありません。** 開発者が手動で削除しない限り、データは保存され続けます。

## 4. アクセスできる人

送信されたデータには、開発者(pandrabox / 大崎商会)のみが管理用の認証情報を用いてアクセスできます。加えて、インフラを提供するCloudflare、および通知メールの配信を行うResendが、サービス提供に必要な範囲で技術的にデータを扱います(いずれも一般的なクラウド・メール配信事業者です)。

## 5. お問い合わせ

このポリシーに関するご質問、データの確認・削除等のご依頼は、アプリ内の「問合せ」ボタン、または本プロジェクトのGitHubリポジトリのIssueからご連絡ください。

## 6. 改定

本ポリシーは、本ツールの実装変更に応じて更新されることがあります。
